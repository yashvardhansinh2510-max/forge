import { PipelineClient } from '@/components/crm/pipeline/pipeline-client'

export const metadata = { title: 'Pipeline — Forge CRM' }

export default function PipelinePage() {
  return <PipelineClient />
}
