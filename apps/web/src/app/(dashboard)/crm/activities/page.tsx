import { ActivitiesClient } from '@/components/crm/activities/activities-client'

export const metadata = { title: 'Activities — Forge CRM' }

export default function ActivitiesPage() {
  return <ActivitiesClient />
}
