import { CompaniesClient } from '@/components/crm/companies/companies-client'

export const metadata = { title: 'Companies — Forge CRM' }

export default function CompaniesPage() {
  return <CompaniesClient />
}
