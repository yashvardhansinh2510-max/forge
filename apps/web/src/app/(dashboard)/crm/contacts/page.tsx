import { ContactsClient } from '@/components/crm/contacts/contacts-client'

export const metadata = { title: 'Contacts — Forge CRM' }

export default function ContactsPage() {
  return <ContactsClient />
}
