import { SavedQuotationsList } from '@/components/pos/saved-quotations-list'

export const metadata = { title: 'Quotations — Forge' }

export default function QuotationsPage() {
  return <SavedQuotationsList />
}
