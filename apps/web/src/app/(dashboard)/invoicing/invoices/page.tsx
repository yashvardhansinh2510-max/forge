import { InvoicesClient } from '@/components/invoicing/invoices/invoices-client'

export const metadata = { title: 'Invoices — Forge' }

export default function InvoicesPage() {
  return <InvoicesClient />
}
