import { PaymentsClient } from '@/components/invoicing/payments/payments-client'

export const metadata = { title: 'Payments — Forge' }

export default function PaymentsPage() {
  return <PaymentsClient />
}
