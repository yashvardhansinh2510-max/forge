import { CatalogueClient } from '@/components/catalogue/catalogue-client'
export const metadata = { title: 'Catalogue — Forge' }
export default function CataloguePage() { return <CatalogueClient /> }
