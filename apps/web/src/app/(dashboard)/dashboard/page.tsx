import { DashboardClient } from '@/components/dashboard/dashboard-client'

export const metadata = {
  title: 'Dashboard — Forge',
}

export default function DashboardPage() {
  return <DashboardClient firstName="" />
}
